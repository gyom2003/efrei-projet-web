schema: http://localhost:3000/graphql

documents: ./src/user/queries/**/*.graphql

generates:
  ./generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-client-helpers
